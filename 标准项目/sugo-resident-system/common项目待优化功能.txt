1-请求DTO数据量过大，各个类套用不仅仅造成对象大，且不是单例
   能不能把各个busi设计成单例？学spring
   数据传递不在用T，而是借用线程全局变量，在线程维度解决数据问题
   
2-目前busi使用不支持多线程，我们根据每个类的特点发现：
function内的action往往可以并发操作
process内的funtion往往需要同步
能不能设计一个能使得目的busi支持并发操作

3-如果一个类action核心方法只是单一处理一件事，比如检查字段为空，但是别人又想复用，能不能设置成通用类，方便他人复用，但是又不能破坏封装

4-spring中常见常量@value注解获取，要采用替换的方式，目前没有解决方法，，可以试试注入工具类，工具类来初始化
   